{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "overlay", "screenshot_toast"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
